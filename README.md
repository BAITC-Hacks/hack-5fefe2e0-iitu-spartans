# Voice Router — IITU+SPARTANS · HackAlem AI 2026

Трек: **Halyk Bank — кейс 2: Voice Router**. Голосовой ИИ-агент контакт-центра (по данным кейса — страховая Saqta Insurance), который выбирает один из 40 сценариев с помощью LLM-слоя и разговаривает на казахском и русском, в том числе на смешанной речи.

> Статус: каркас репозитория. Логика роутера и исполнителя сценариев ещё не реализована.

## Команда
Все участники — International Information Technology University (IITU), Алматы.

| Участник | Telegram |
|---|---|
| Сырым Жакыпбеков (лидер) | @syrym95kz |
| Нұрхан Батырхан | @batyrnurhan |
| Мусилимов Жандаулет | @zh_11_an |

## Проблема
_TODO: для кого и какую боль решаем (контакт-центр, 40 сценариев, kk/ru и смешанная речь, срочные случаи, необратимые действия)._

## Решение
_TODO: коротко, что делает агент и чем выбор сценария через LLM лучше классификатора намерений._

## Архитектура
_TODO: схема._ Планируемый конвейер (по референсу кейса): STT → триаж (язык, нормализация, срочность) → LLM-роутер → политика решений → исполнитель сценария (FSM) → ответ → TTS, плюс общее состояние диалога.

```
backend/          FastAPI (Python 3.11+)
  app/config.py   настройки из env
  app/dataset.py  загрузка датасета кейса и проверка связей между файлами
  app/router/     LLM-роутер (в работе)
  app/executor/   исполнитель сценариев (в работе)
  app/main.py     API, /health
frontend/         веб-интерфейс (в работе)
scripts/          утилиты: прогон dev-набора и т.п. (в работе)
case_2/voice_router_dataset/   стартовый кит организаторов (не изменяется)
```

## Функции
_TODO._

## Стек
Python 3.11+, FastAPI, Pydantic, OpenAI API. _TODO: STT/TTS, фронтенд._

## Запуск одной командой
```bash
cp .env.example .env   # заполнить OPENAI_API_KEY
docker compose up --build
curl localhost:8000/health
```

Локально без Docker:
```bash
python -m venv .venv && .venv/bin/pip install -r backend/requirements.txt
cd backend && ../.venv/bin/uvicorn app.main:app --reload
```

## Оценка точности
_TODO: скрипт прогона роутера по `dev_utterances.json` → `predictions.json`, затем_
```bash
python case_2/voice_router_dataset/evaluate.py predictions.json case_2/voice_router_dataset/dev_utterances.json
```
_Результаты: primary_accuracy / full_match / intent_recall по языкам и типам._

## Трассировка
_TODO: после каждой реплики показываем язык, выбранные сценарии с confidence, альтернативы, слоты, действия и `latency_ms` по этапам._

## Границы применимости
_TODO: данные синтетические; «сегодня» = 2026-10-01 (дата среза датасета); моки бэкенда вместо реальных систем; что не поддерживается._

## Использованные материалы
- **Стартовый кит организаторов** (`case_2/voice_router_dataset/`: сценарии, слоты, действия, база знаний, мок-бэкенд, примеры диалогов, dev-набор, `evaluate.py`) — чужой материал, получен на старте хакатона и используется без изменений.
- `case_2/ANALYSIS.md` — наш разбор стартового кита.
- Ранее созданных собственных наработок в проекте нет. _Обновить, если появятся (п.6.4 Положения)._
- Разработка ведётся с использованием Codex (требование хакатона).
